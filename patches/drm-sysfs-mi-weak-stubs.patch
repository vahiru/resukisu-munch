diff --git a/drivers/gpu/drm/drm_sysfs.c b/drivers/gpu/drm/drm_sysfs.c
index 4adbe32c5f05..f61f98dac12d 100644
--- a/drivers/gpu/drm/drm_sysfs.c
+++ b/drivers/gpu/drm/drm_sysfs.c
@@ -25,6 +25,119 @@
 #define to_drm_minor(d) dev_get_drvdata(d)
 #define to_drm_connector(d) dev_get_drvdata(d)
 
+int __weak dsi_display_set_disp_param(struct drm_connector *connector,
+				u32 param_type)
+{
+	return -EOPNOTSUPP;
+}
+
+int __weak dsi_display_get_disp_param(struct drm_connector *connector,
+				u32 *param_type)
+{
+	if (param_type)
+		*param_type = 0;
+
+	return -EOPNOTSUPP;
+}
+
+ssize_t __weak dsi_display_write_mipi_reg(struct drm_connector *connector,
+				char *buf)
+{
+	return -EOPNOTSUPP;
+}
+
+ssize_t __weak dsi_display_read_mipi_reg(struct drm_connector *connector,
+				char *buf)
+{
+	return -EOPNOTSUPP;
+}
+
+ssize_t __weak dsi_display_read_oled_pmic_id(struct drm_connector *connector,
+				char *buf)
+{
+	return -EOPNOTSUPP;
+}
+
+ssize_t __weak dsi_display_read_panel_info(struct drm_connector *connector,
+				char *buf)
+{
+	return -EOPNOTSUPP;
+}
+
+ssize_t __weak dsi_display_read_wp_info(struct drm_connector *connector,
+				char *buf)
+{
+	return -EOPNOTSUPP;
+}
+
+ssize_t __weak dsi_display_read_dynamic_fps(struct drm_connector *connector,
+				char *buf)
+{
+	return -EOPNOTSUPP;
+}
+
+int __weak dsi_display_set_doze_brightness(struct drm_connector *connector,
+				int doze_brightness)
+{
+	return -EOPNOTSUPP;
+}
+
+ssize_t __weak dsi_display_get_doze_brightness(struct drm_connector *connector,
+				char *buf)
+{
+	return -EOPNOTSUPP;
+}
+
+int __weak dsi_display_read_gamma_param(struct drm_connector *connector)
+{
+	return -EOPNOTSUPP;
+}
+
+ssize_t __weak dsi_display_print_gamma_param(struct drm_connector *connector,
+				char *buf)
+{
+	return -EOPNOTSUPP;
+}
+
+ssize_t __weak dsi_display_fod_get(struct drm_connector *connector, char *buf)
+{
+	return -EOPNOTSUPP;
+}
+
+ssize_t __weak complete_commit_time_get(struct drm_connector *connector,
+				char *buf)
+{
+	return -EOPNOTSUPP;
+}
+
+int __weak dsi_display_set_thermal_hbm_disabled(struct drm_connector *connector,
+				bool thermal_hbm_disabled)
+{
+	return -EOPNOTSUPP;
+}
+
+int __weak dsi_display_get_thermal_hbm_disabled(struct drm_connector *connector,
+				bool *thermal_hbm_disabled)
+{
+	if (thermal_hbm_disabled)
+		*thermal_hbm_disabled = false;
+
+	return -EOPNOTSUPP;
+}
+
+ssize_t __weak dsi_display_get_hw_vsync_info(struct drm_connector *connector,
+				char *buf)
+{
+	return -EOPNOTSUPP;
+}
+
+ssize_t __weak smart_fps_value_show(struct device *device,
+			   struct device_attribute *attr,
+			   char *buf)
+{
+	return -EOPNOTSUPP;
+}
+
 
 /**
  * DOC: overview
